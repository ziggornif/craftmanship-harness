# Glossary

> Maintained by `ubiquitous-language`. Domain terms only — no implementation detail.
> Path recorded in `.harness/config.yml` so all skills find it without hardcoded convention.

<!-- One term per entry. Within a bounded context, one word = one meaning.
     Cross-context splits go in separate glossary files under docs/glossary/<context>.md,
     with docs/glossary/README.md as the context map. -->
