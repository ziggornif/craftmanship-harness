# Architecture

> High-level overview: topology, key structural decisions, context for ADRs.
> Hard-to-reverse, surprising, trade-off decisions go in `docs/adr/` instead.

## Topology
<!-- Hexagonal + API/SPI + vertical slices. Crate/package layout. -->

## Key constraints
<!-- Decisions that shape everything else (e.g. no framework in domain, dyn Trait at composition root). -->

## Slice inventory
<!-- One line per use-case slice: name, responsibility, key SPI ports it declares. -->

## Composition root
<!-- How adapters are wired: where, by what, at startup. -->
